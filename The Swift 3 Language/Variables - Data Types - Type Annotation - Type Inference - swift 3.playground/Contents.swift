//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"


// declaring a constant with the keyword let

let maximumNumberOfwithdrawals = 5

// declaring a variable with the keyword var

var withdrawalAttempts = 3

// declaring multiple variables on a single line separated by a comma
var a = 2, b = 0, c = 1

// declaring multiple Constance on a single line separated by a comma

let d = 2.2, e = 1.1, f = 3.3

// declaring a string with type annotation

var helloSwift3: String

// declaring multiple value with type annotation on a single line

var north, south, east, west: String

// using characters and setting a value to them

let ℃℉ = "Temperature"

let 🐈🐭 = "catMouse"

// changing the value of a variable

var hola = "Hello"
hola = "Hi"

print(hola)

// creating a variable with type inference

var name = "Robert"

// embedding a variable in a string with string interpolation inside the print function

print("His name is \(name)")

// using comments
//we use 2 back slashes to start the comments


/* 
   we can write comments accross multiple lines this way
   with 2 forward slashes and asterisks

*/

// declaring multiple variables on a single line separated bya semicolon
let cat = "🐱" ; print(cat)

// declaring an integer with type annotation

var temperature: Int = 85

// declaring a double with type annotation

var longitude: Double = 5.345643

// declaring a variable with type inference

let three = 3

// declaring a double with type inference

let fraction = 0.14159

// changing the value of an integer to a double, and adding another double to it

let pi = Double(three) + fraction

// changing the value of a double to an integer

let integerPi = Int(pi)

// creating a bool using type inference

let sunnyDay = true

let isRaining = false

// using an if statement to check if the bool is true or false

if sunnyDay {
    print("Beach day")
} else {
    print("Catch up on some reading")
}















